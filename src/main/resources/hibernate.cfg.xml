<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class.class">com.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://ec2-52-212-228-71.eu-west-1.compute.amazonaws.com:5432/dmdip3abvihcu</property>
        <property name="connection.username">airiwykspylour</property>
        <property name="connection.password">fad950ca251a17ebc63320cfd5c6b3de7b5adc66ca048c8a4bf0dcd2a3cd6c1e</property>
        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">1</property>

        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQL10Dialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>

    </session-factory>

</hibernate-configuration>